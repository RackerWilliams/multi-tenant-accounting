To build the doc you'll need to install fop and graphviz.
On a mac:
    sudo port install fop
    sudo port install graphviz

Then you should be able to do:

    make view 

to generate the PDF and display it.  
